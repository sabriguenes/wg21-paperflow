[project]
name = "agora"
version = "0.1.0"
description = "WG21 paper thread planning pipeline (analysis phase of the Mod)"
requires-python = ">=3.12"
license = {text = "BSL-1.0"}
authors = [{name = "Vinnie Falco", email = "vinnie.falco@gmail.com"}]
dependencies = [
    "paperstore",
    "pipeline",
    "pydantic-ai",
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agora"]

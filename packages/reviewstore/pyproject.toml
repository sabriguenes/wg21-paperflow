[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "reviewstore"
version = "0.1.0"
description = "SQLite-backed cache of review pipeline analysis results"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/reviewstore"]
